<%= form_for @presence_to_update , :url => { :action => "update" , :controller=>"presences" } do |form| %>
  <% if @presence_to_update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@presence_to_update.errors.count, "error") %> prohibited this presence from being saved:</h2>

      <ul>
        <% @presence_to_update.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" hidden="">
    <%= form.label :checkout %>
    <%= form.datetime_select :checkout %>
  </div>

    <%= button_tag 'Checkout', class: "checkout-button d-none d-sm-inline-block btn btn-sm btn-danger shadow-sm" %>
<% end %>